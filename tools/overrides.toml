# overrides.toml — project-specific prose overrides.
#
# Copy this file to the parent tools/ directory:
#   cp tools/docgen/overrides.toml.example tools/overrides.toml
#
# tools/overrides.toml is gitignored — never commit it.
#
# Keys match the header path relative to include/, without extension,
# using forward slashes. Example:
#   "vision/implementation/detection/obstacle_detector"
#
# Within each section:
#   __overview__           = overview paragraph (Markdown)
#   __constructor__        = constructor description
#   method_name            = description for that method
#   method_name.param_name = description for a specific parameter
#
# Methods already handled by naming conventions (get_*/set_*,
# on_setup, on_teardown, on_update, setup, teardown, …) do NOT
# need entries here unless you want to override the generated text.
#
# Namespace overviews can be provided under:
#
[__namespaces__]
core = "The `acs::core` namespace defines the base object model for ACS. It provides `i_object` for shared identity (a debug-facing tag/name) and `i_threaded_object` for managed periodic execution in derived objects."
gui = "The `acs::gui` namespace contains GUI objects and interfaces used to host windows, register widgets, and render runtime visualization views."
opengl = "The `acs::opengl` namespace contains OpenGL-focused rendering objects and helpers used by the visualization stack."
utility = "The `acs::utility` namespace contains utility objects and interfaces for shared services such as configuration parsing and Zenoh connectivity."
vision = "The `acs::vision` namespace contains perception objects and interfaces for camera access, floor estimation, obstacle detection, and traffic-light state inference using ZED stereo data."
#
# ---------------------------------------------------------------------------
# Example:
# ---------------------------------------------------------------------------
#
# ["mynamespace/implementation/my_class"]
# __overview__    = "Does X and Y."
# __constructor__ = "Creates a my class with the given name."
# my_method       = "Performs the thing."
# "my_method.param_name" = "The value to use."

["vision/implementations/camera"]
__overview__ = "Concrete `i_camera` implementation backed by ZED SDK capture. It acquires RGB and depth frames, tracks stream health metrics, and exposes the native camera handle for lower-level integrations."
__constructor__ = "Creates a camera component that initializes and manages the ZED capture pipeline."
"__constructor__.update_rate" = "Requested update frequency in Hz for frame acquisition and status refresh."
"__constructor__.parameters" = "Camera configuration bundle (resolution, depth mode, runtime options, and related capture settings)."
update = "Polls the camera, updates color and depth frame buffers, and refreshes stream status metrics for downstream consumers."
resolution_t = { __description__ = "Supported camera resolution presets.", vga = "VGA resolution.", svga = "SVGA resolution.", hd720 = "HD720 resolution.", hd1080 = "HD1080 resolution." }
depth_mode_t = { __description__ = "Supported ZED depth presets.", none = "No depth estimation.", neural_light = "Lightweight neural depth.", neural = "Standard neural depth.", neural_plus = "Highest-quality neural depth." }
parameters_t = { __description__ = "Camera configuration values used when opening and running the stream.", resolution = "Capture resolution preset.", depth_mode = "Depth processing preset.", device_fps = "Requested device frame rate.", enable_verbose_sdk_logging = "Enables verbose ZED SDK logging.", depth_minimum_distance = "Minimum accepted depth distance in meters.", depth_maximum_distance = "Maximum accepted depth distance in meters.", confidence_threshold = "Depth confidence cutoff.", texture_confidence_threshold = "Texture confidence cutoff." }

["vision/implementations/floor_detector"]
__overview__ = "Concrete `i_floor_detector` implementation that estimates the dominant floor plane from camera data and publishes plane geometry for obstacle reasoning."
__constructor__ = "Creates a floor detector bound to a camera source and periodic update loop."
"__constructor__.update_rate" = "Requested detection frequency in Hz for floor-plane estimation."
"__constructor__.camera_ptr" = "Shared camera dependency that provides synchronized RGB/depth data used during floor extraction."
update = "Processes the latest camera data to detect and cache floor-plane geometry and detection state."

["vision/implementations/obstacle_detector"]
__overview__ = "Concrete `i_obstacle_detector` implementation that fuses camera data and floor-plane estimation to identify obstacles in the scene."
__constructor__ = "Creates an obstacle detector that runs the obstacle extraction pipeline at a fixed update rate."
"__constructor__.update_rate" = "Requested detection frequency in Hz for obstacle analysis."
"__constructor__.parameters" = "Obstacle-detection configuration bundle (thresholds, filtering, and model-specific runtime settings)."
"__constructor__.camera_ptr" = "Shared camera dependency that provides the live RGB/depth frames used for obstacle inference."
"__constructor__.floor_detector_ptr" = "Shared floor-detector dependency used to mask floor regions and improve obstacle separation."
update = "Runs one obstacle-detection cycle and updates cached detection outputs for consumers."
parameters_t = { __description__ = "Obstacle detection settings used during scene processing.", obstacle_min_range = "Minimum obstacle range in meters.", obstacle_max_range = "Maximum obstacle range in meters.", obstacle_height_threshold = "Minimum height above the floor used to classify obstacles.", min_contour_area = "Minimum contour area kept as an obstacle candidate.", road_width = "Nominal road width in meters used by zoning and obstacle interpretation." }

["vision/implementations/traffic_light_detector"]
__overview__ = "Concrete `i_traffic_light_detector` implementation that analyzes camera imagery with a DNN model and obstacle context to determine red-light state."
__constructor__ = "Creates a traffic-light detector with update cadence, model configuration, and shared perception dependencies."
"__constructor__.update_rate" = "Requested detection frequency in Hz for traffic-light inference."
"__constructor__.parameters" = "Traffic-light detector configuration bundle (model settings and decision thresholds)."
"__constructor__.camera" = "Shared camera dependency that provides live frames for traffic-light analysis."
"__constructor__.obstacle_detector" = "Shared obstacle-detector dependency used for scene context and candidate filtering."
update = "Runs one traffic-light detection cycle and updates cached red-light state."
parameters_t = { __description__ = "Traffic-light detection settings used during model inference and state debouncing.", model_params = "DNN model configuration used to load and run traffic-light inference.", confidence_threshold = "Minimum detection confidence required to accept a traffic-light prediction.", red_light_class_id = "Model class ID interpreted as a red traffic light.", distance_threshold = "Maximum scene distance in meters considered for red-light candidates.", on_threshold = "Consecutive positive detections required to switch state to red-light active.", off_threshold = "Consecutive negative detections required to clear the red-light active state." }

["gui/implementations/glfw_window"]
__overview__ = "Concrete `i_glfw_window` implementation that owns the native GLFW window and applies window/runtime options from project configuration."
__constructor__ = "Creates a GLFW window wrapper from the provided runtime window parameters."
"__constructor__.parameters" = "Window configuration bundle (size, title behavior, context options, and related GLFW settings)."
parameters_t = { __description__ = "Window creation options used for the GLFW host window.", title = "Initial window title.", width = "Initial window width in pixels.", height = "Initial window height in pixels.", start_maximized = "Starts the window maximized when true." }

["gui/implementations/widget_controller"]
__overview__ = "Concrete `i_widget_controller` implementation that owns widget registrations and orchestrates per-frame UI rendering order."
__constructor__ = "Creates a widget controller that manages widget registration and render dispatch."

["gui/implementations/widgets/about_widget"]
__overview__ = "Concrete `widget_host` implementation that renders project/about metadata and runtime informational content in the GUI."
__constructor__ = "Creates an about widget with default title and initial visibility state."
on_render = "Renders the about panel content for the current frame."

["gui/implementations/widgets/camera_widget"]
__overview__ = "Concrete `threaded_widget_host` implementation that visualizes live camera outputs and related stream diagnostics."
__constructor__ = "Creates a camera widget bound to a camera source and periodic refresh loop."
"__constructor__.update_rate" = "Requested widget update frequency in Hz for refreshing camera-derived content."
"__constructor__.camera" = "Shared camera dependency that provides the latest frames and camera status data."
update = "Refreshes cached camera-derived view state used by the widget renderer."
on_render = "Draws camera imagery and related diagnostic overlays in the widget UI."

["gui/implementations/widgets/obstacle_diagnostic_widget"]
__overview__ = "Concrete `threaded_widget_host` implementation that visualizes obstacle-detection diagnostics, including union regions and zone-level occupancy results."
__constructor__ = "Creates an obstacle diagnostic widget bound to camera and obstacle-detector dependencies with a periodic refresh loop."
"__constructor__.update_rate" = "Requested widget update frequency in Hz for refreshing obstacle diagnostics."
"__constructor__.camera_ptr" = "Shared camera dependency that provides the latest imagery used by the diagnostic view."
"__constructor__.obstacle_detector_ptr" = "Shared obstacle-detector dependency that provides zone and union-detection outputs."
update = "Refreshes cached obstacle-analysis state used by the widget renderer."
on_render = "Draws obstacle diagnostics, including zone overlays and union obstacle previews, in the widget UI."

["gui/implementations/widget_host"]
__overview__ = "Base host implementation for `i_widget` that provides common widget state (title/open flag) and delegates widget-specific drawing to derived classes."
__constructor__ = "Creates a widget host with a stable identity tag and display title."
"__constructor__.tag" = "Unique component tag used for logging and lifecycle identification."
"__constructor__.title" = "Display title shown in the widget header or window chrome."
on_render = "Implements widget-specific rendering logic invoked by the base `render` flow."

["gui/implementations/threaded_widget_host"]
__overview__ = "Base host implementation for `i_threaded_widget` that combines threaded update behavior with widget rendering helpers."
__constructor__ = "Creates a threaded widget host with update cadence and UI title metadata."
"__constructor__.tag" = "Unique component tag used for logging and lifecycle identification."
"__constructor__.update_rate" = "Requested update frequency in Hz for the threaded widget loop."
"__constructor__.title" = "Display title shown in the widget header or window chrome."
on_render = "Implements widget-specific rendering logic invoked by the threaded host render flow."

["utility/implementations/toml_reader"]
__overview__ = "Concrete `i_toml_reader` implementation that loads and owns parsed TOML configuration for runtime component setup."
__constructor__ = "Creates a TOML reader initialized with an initial configuration file path."
"__constructor__.file_path" = "Path to the TOML configuration file that should be parsed."

["utility/implementations/gps"]
__overview__ = "Concrete `i_gps` implementation that reads NMEA and UBX input from a serial GPS receiver, converts positions for downstream consumers, and exposes the latest parsed fix."
__constructor__ = "Creates a GPS reader configured for a serial device and its communication settings."
"__constructor__.params" = "GPS reader configuration bundle (serial port name and baud rate)."
parameters_t = { __description__ = "GPS reader configuration values used to open the receiver connection.", port_name = "Serial port name used to reach the GPS receiver.", baud_rate = "Serial baud rate used for GPS communication." }

["utility/implementations/zenoh_client"]
__overview__ = "Concrete `i_zenoh_client` implementation that manages Zenoh endpoint settings and associated session/config handles."
__constructor__ = "Creates a Zenoh client with the specified remote endpoint address and port."
"__constructor__.address" = "Remote Zenoh endpoint address (hostname or IP)."
"__constructor__.port" = "Remote Zenoh endpoint port number."

["gui/interfaces/i_widget_controller"]
__overview__ = "Interface that defines widget registration and frame render orchestration for GUI controllers."
register_widget = "Registers a non-threaded widget with the controller so it is included in render traversal."
"register_widget.widget_ptr" = "Owning pointer to the widget instance being transferred to controller management."
register_threaded_widget = "Registers a threaded widget with the controller so it is included in render traversal."
"register_threaded_widget.widget_ptr" = "Owning pointer to the threaded widget instance being transferred to controller management."
render = "Renders all registered widgets in controller-defined order for the current frame."
stop_all_threaded_widgets = "Stops all registered threaded widgets and ensures their worker loops are shut down safely."

["utility/interfaces/i_toml_reader"]
__overview__ = "Interface for loading, owning, and exposing parsed TOML configuration tables."
get_file_path = "Returns the currently configured TOML file path used by the reader."
set_file_path = "Updates the TOML file path to be used on the next parse operation."
"set_file_path.file_path" = "New TOML file path to load when parsing is triggered."
get_table_ref = "Returns mutable access to the parsed TOML root table."

["utility/interfaces/i_gps"]
__overview__ = "Interface for reading GPS receiver data, parsing supported message formats, and exposing the latest position fix and conversion helpers."
parse_nmea = "Parses a single NMEA sentence from the GPS receiver stream."
"parse_nmea.sentence" = "NMEA sentence to parse."
parse_ubx = "Parses a UBX binary frame from the GPS receiver stream."
"parse_ubx.buffer" = "Raw UBX payload bytes to parse."
get_latest_point = "Returns the most recently parsed GPS point."
convert_to_utm = "Converts the provided latitude and longitude to UTM coordinates."
"convert_to_utm.lat" = "Latitude in decimal degrees."
"convert_to_utm.lon" = "Longitude in decimal degrees."
load_reference_points = "Loads a list of reference GPS points from a file."
"load_reference_points.file_path" = "Path to the reference-point file to load."
read_point = "Reads a single GPS point by index from a file."
"read_point.index" = "Zero-based point index to read."
"read_point.file_path" = "Path to the GPS point file to read from."

["utility/interfaces/i_zenoh_client"]
__overview__ = "Interface for configuring Zenoh connection endpoints and exposing session/config handles."
get_address = "Returns the configured remote Zenoh endpoint address."
set_address = "Updates the remote Zenoh endpoint address used for session setup."
"set_address.address" = "Remote Zenoh endpoint address (hostname or IP)."
get_port = "Returns the configured remote Zenoh endpoint port."
set_port = "Updates the remote Zenoh endpoint port used for session setup."
"set_port.port" = "Remote Zenoh endpoint port number."
get_session_ptr = "Returns a pointer to the active Zenoh session object, if initialized."
get_config_ptr = "Returns a pointer to the active Zenoh configuration object, if initialized."

["vision/interfaces/i_camera"]
__overview__ = "Interface that defines camera stream access for RGB/depth frames, camera metadata, and runtime health metrics."
get_color_frame = "Returns the most recent color frame stored in GPU memory."
get_depth_frame = "Returns the most recent depth frame stored in GPU memory."
get_model = "Returns the camera model identifier reported by the backend."
get_fps = "Returns the current effective capture frame rate."
get_dropped_frames_count = "Returns the number of frames dropped by the capture pipeline."
get_is_opened = "Returns whether the underlying camera stream is currently open and usable."
get_zed_camera_ref = "Returns a reference to the underlying ZED camera object for advanced operations."
get_intrinsics = "Returns the active camera intrinsic calibration values used for geometric projection and back-projection."

["vision/interfaces/i_floor_detector"]
__overview__ = "Interface that defines floor-plane detection outputs derived from live camera input."
get_detected_floor_plane = "Returns the latest detected floor plane object from the backend."
get_plane_equation = "Returns the latest floor plane equation coefficients."
get_is_floor_detected = "Returns whether a valid floor plane was detected in the latest update cycle."

["vision/interfaces/i_obstacle_detector"]
__overview__ = "Interface that defines obstacle-detection outputs derived from camera and scene analysis pipelines."
get_floor_detector_ptr = "Returns the floor-detector dependency used by the obstacle pipeline."
get_obstacle_min_range = "Returns the minimum obstacle range threshold used for filtering detections."
get_obstacle_max_range = "Returns the maximum obstacle range threshold used for filtering detections."
get_obstacle_height_threshold = "Returns the height threshold used to classify points as obstacle candidates above the floor plane."
get_road_width = "Returns the configured road-width estimate used by zone-based obstacle interpretation."
get_union_box = "Returns the bounding box that encloses all currently detected obstacle regions."
get_union_crop = "Returns the cropped image region corresponding to the current union obstacle bounding box."
get_zone_results = "Returns the latest per-zone obstacle-analysis results keyed by road zone."
get_left_zone = "Returns the latest obstacle-analysis result for the left scene zone."
get_center_zone = "Returns the latest obstacle-analysis result for the center scene zone."
get_right_zone = "Returns the latest obstacle-analysis result for the right scene zone."
get_horizon_y = "Returns the latest estimated horizon row (in image pixels) used by obstacle zoning."

["vision/interfaces/i_traffic_light_detector"]
__overview__ = "Interface that defines traffic-light state outputs produced by a threaded perception pipeline."
is_red_light_detected = "Returns whether a red traffic light is currently considered detected by the pipeline."

["vision/utility/floor_plane_math"]
__overview__ = "Utility helpers for floor-plane geometry math used by vision components when normalizing plane orientation and computing camera-space distances."
ensure_plane_faces_up = "Normalizes a plane equation so its normal points upward in camera/world convention."
"ensure_plane_faces_up.equation" = "Input plane equation coefficients `(a, b, c, d)` to normalize."
calculate_normal_magnitude = "Computes the Euclidean magnitude of the plane normal `(a, b, c)`."
"calculate_normal_magnitude.plane" = "Plane equation coefficients `(a, b, c, d)` whose normal magnitude is needed."
back_project_pixel_to_3d = "Back-projects a depth pixel into 3D camera coordinates using camera intrinsics."
"back_project_pixel_to_3d.x" = "Pixel x-coordinate in image space."
"back_project_pixel_to_3d.y" = "Pixel y-coordinate in image space."
"back_project_pixel_to_3d.z" = "Depth value at the pixel in camera depth units."
"back_project_pixel_to_3d.intrinsics" = "Camera intrinsic parameters (`fx`, `fy`, `cx`, `cy`) used for back-projection."
project_3d_to_pixel = "Projects a 3D camera-space point onto image coordinates using camera intrinsics."
"project_3d_to_pixel.point" = "3D point in camera coordinates to project into image space."
"project_3d_to_pixel.intrinsics" = "Camera intrinsic parameters (`fx`, `fy`, `cx`, `cy`) used for projection."
get_distance_to_plane = "Computes signed point-to-plane distance, normalized by the provided plane normal magnitude."
"get_distance_to_plane.point" = "3D point to evaluate against the plane equation."
"get_distance_to_plane.plane" = "Plane equation coefficients `(a, b, c, d)` used for distance evaluation."
"get_distance_to_plane.plane_normal_length" = "Precomputed magnitude of the plane normal used for normalization."

["vision/utility/dnn_model"]
__overview__ = "Utility wrapper for OpenCV DNN inference used by perception components to run model forward passes on CPU/GPU-backed image tensors."
__constructor__ = "Creates a DNN model wrapper from the provided model/runtime configuration."
"__constructor__.parameters" = "DNN model configuration bundle (network paths, input shape, and preprocessing options)."
forward = "Runs a forward inference pass and returns model output activations for the provided input tensor."
"forward.input" = "Input image tensor (CPU or CUDA-backed) to preprocess and feed into the model."
parameters_t = { __description__ = "DNN model configuration values used to load the network and preprocess inputs.", model_path = "Path to the model weights file.", config_path = "Path to the model configuration/prototxt file when required by the backend.", input_width = "Model input width in pixels.", input_height = "Model input height in pixels.", scale_factor = "Scalar multiplier applied during blob preprocessing.", mean = "Per-channel mean values subtracted during input normalization.", swap_rb = "Swaps red/blue channels during preprocessing when true.", crop = "Crops the resized image during blob creation when true." }

["vision/types/camera_intrinsics"]
__overview__ = "Camera intrinsic calibration values used by vision utilities for pixel back-projection and geometric measurements."
camera_intrinsics = { __description__ = "Intrinsic camera parameters in pixel units.", focal_length_x = "Horizontal focal length (`fx`) in pixels.", focal_length_y = "Vertical focal length (`fy`) in pixels.", center_point_x = "Principal-point x-coordinate (`cx`) in pixels.", center_point_y = "Principal-point y-coordinate (`cy`) in pixels." }

["vision/types/zone"]
__overview__ = "Zone classification types used by obstacle analysis to summarize free/occupied state in left, center, and right road regions."

["gui/interfaces/i_widget"]
__overview__ = "Interface that defines the minimal render contract for GUI widgets."
get_title = "Returns the display title used when rendering the widget container."
get_is_open_ref = "Returns a mutable reference to the widget visibility flag."

["gui/interfaces/i_threaded_widget"]
__overview__ = "Interface that defines the render contract for widgets that also run periodic threaded updates."
get_title = "Returns the display title used when rendering the widget container."
get_is_open_ref = "Returns a mutable reference to the widget visibility flag."

["gui/interfaces/i_glfw_window"]
__overview__ = "Interface that exposes access to the native GLFW window handle used by the GUI runtime."
get_window_ptr = "Returns a pointer to the underlying GLFW window instance."

["core/implementations/object"]
__overview__ = "Concrete `i_object` base implementation that provides stable component identity via a tag and serves as the root for non-threaded ACS components."
__constructor__ = "Creates a base object with a unique component tag."
"__constructor__.tag" = "Unique component tag used for logging, diagnostics, and lifecycle identification."

["core/implementations/threaded_object"]
__overview__ = "Concrete `i_threaded_object` base implementation that provides a managed periodic update loop for derived components."
__constructor__ = "Creates a threaded object with identity metadata and initial loop frequency."
"__constructor__.tag" = "Unique component tag used for logging, diagnostics, and lifecycle identification."
"__constructor__.update_rate" = "Requested update-loop frequency in Hz."
update = "Performs one derived-class update iteration invoked by the internal thread loop."

["core/interfaces/i_object"]
__overview__ = "Base interface for all ACS components, providing shared identity and lifecycle integration points."
get_tag = "Returns the component tag used for identification in logs, diagnostics, and component wiring."

["core/interfaces/i_threaded_object"]
__overview__ = "Interface for components that execute periodic work on a managed background thread."
start = "Starts the component's background update loop."
stop = "Stops the component's background update loop and joins associated worker resources."
get_update_rate = "Returns the configured update-loop frequency in Hz."
set_update_rate = "Updates the target update-loop frequency in Hz."
"set_update_rate.update_rate" = "New update-loop frequency in Hz."
get_is_running = "Returns whether the background update loop is currently running."

["opengl/pixel_unpack_buffer"]
__overview__ = "OpenGL Pixel Unpack Buffer (PBO) helper for efficient CUDA-to-OpenGL transfer workflows. It manages PBO allocation, CUDA graphics registration, and texture upload orchestration."
generate = "Creates and initializes a pixel unpack buffer sized for the specified frame dimensions, and registers it with CUDA."
"generate.buffer_id" = "Output OpenGL buffer object ID for the generated PBO."
"generate.width" = "Target frame width in pixels."
"generate.height" = "Target frame height in pixels."
"generate.cuda_pbo_resource" = "Output CUDA graphics resource handle associated with the generated PBO."
bind = "Binds the pixel unpack buffer to `GL_PIXEL_UNPACK_BUFFER` for subsequent upload operations."
"bind.buffer_id" = "OpenGL buffer object ID of the PBO to bind."
unbind = "Unbinds the current pixel unpack buffer from `GL_PIXEL_UNPACK_BUFFER`."
free = "Releases the pixel unpack buffer and any associated OpenGL resources."
"free.buffer_id" = "OpenGL buffer object ID of the PBO to release."
"free.cuda_pbo_resource" = "CUDA graphics resource handle associated with the PBO to unregister and release."
resize = "Reallocates the pixel unpack buffer for new dimensions and refreshes CUDA registration state."
"resize.buffer_id" = "OpenGL buffer object ID of the PBO to resize."
"resize.width" = "New target frame width in pixels."
"resize.height" = "New target frame height in pixels."
"resize.cuda_pbo_resource" = "CUDA graphics resource handle to update/re-register for the resized PBO."
to_opengl_texture = "Copies source image data through the PBO path and updates the target OpenGL texture."
"to_opengl_texture.buffer_id" = "OpenGL buffer object ID of the PBO used as transfer staging memory."
"to_opengl_texture.texture_id" = "OpenGL texture object ID that receives the uploaded pixel data."
"to_opengl_texture.width" = "Image width in pixels."
"to_opengl_texture.height" = "Image height in pixels."
"to_opengl_texture.src_pitch" = "Source row stride in bytes."
"to_opengl_texture.cuda_pbo_resource" = "CUDA graphics resource handle bound to the target PBO."
"to_opengl_texture.data" = "Pointer to source pixel data to upload."

["opengl/texture"]
__overview__ = "OpenGL texture helper for allocating, resizing, and updating 2D texture storage used by rendering and visualization pipelines."
generate = "Creates a 2D OpenGL texture object and allocates storage for the specified dimensions."
"generate.texture_id" = "Output OpenGL texture object ID for the generated texture."
"generate.width" = "Initial texture width in pixels."
"generate.height" = "Initial texture height in pixels."
allocate = "Allocates or reallocates texture storage for an existing texture object."
"allocate.texture_id" = "OpenGL texture object ID to allocate."
"allocate.width" = "Texture width in pixels."
"allocate.height" = "Texture height in pixels."
bind = "Binds the texture for subsequent OpenGL operations on the current context."
"bind.texture_id" = "OpenGL texture object ID to bind."
unbind = "Unbinds the current texture from the active texture target."
free = "Deletes the texture object and releases its GPU memory allocation."
"free.texture_id" = "OpenGL texture object ID to delete."
resize = "Resizes texture storage to match new frame dimensions."
"resize.texture_id" = "OpenGL texture object ID to resize."
"resize.width" = "New texture width in pixels."
"resize.height" = "New texture height in pixels."
update_from_pbo = "Updates texture contents from the currently bound pixel unpack buffer."
"update_from_pbo.texture_id" = "OpenGL texture object ID to update."
"update_from_pbo.width" = "Texture width in pixels."
"update_from_pbo.height" = "Texture height in pixels."
update = "Uploads raw pixel data directly into the texture storage."
"update.texture_id" = "OpenGL texture object ID to update."
"update.width" = "Image width in pixels."
"update.height" = "Image height in pixels."
"update.data" = "Pointer to source pixel data to upload."
